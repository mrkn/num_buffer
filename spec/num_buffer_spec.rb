require 'spec_helper'

describe NumBuffer do
  it 'has a version number' do
    expect(NumBuffer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
